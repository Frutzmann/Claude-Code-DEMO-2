---
phase: 05-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/page.tsx
  - src/components/landing/features.tsx
  - src/components/landing/pricing.tsx
  - src/components/landing/testimonials.tsx
  - src/components/landing/cta.tsx
autonomous: false

must_haves:
  truths:
    - "Features section highlights AI generation, speed, and ease of use"
    - "Pricing shows Free (5/mo), Pro ($19, 50/mo), Agency ($49, 200/mo) tiers"
    - "Testimonials section displays social proof placeholders"
    - "Final CTA section prompts signup"
  artifacts:
    - path: "src/components/landing/features.tsx"
      provides: "Features grid with icon cards"
      min_lines: 60
    - path: "src/components/landing/pricing.tsx"
      provides: "Three-tier pricing comparison"
      min_lines: 100
    - path: "src/components/landing/testimonials.tsx"
      provides: "Social proof section"
      min_lines: 40
    - path: "src/components/landing/cta.tsx"
      provides: "Final call-to-action section"
      min_lines: 30
  key_links:
    - from: "src/components/landing/pricing.tsx"
      to: "/signup"
      via: "CTA buttons"
      pattern: 'href="/signup"'
    - from: "src/app/page.tsx"
      to: "landing components"
      via: "import and render"
      pattern: "import.*from.*landing"
---

<objective>
Complete landing page with features, pricing, testimonials, and final CTA sections

Purpose: Fill in all remaining landing page sections to meet requirements LAND-02, LAND-03, LAND-04 and create a complete conversion funnel

Output: Full landing page ready for user acquisition with all sections
</objective>

<execution_context>
@/Users/frutz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frutz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-landing-page/05-RESEARCH.md
@.planning/phases/05-landing-page/05-01-SUMMARY.md
@src/app/page.tsx
@src/components/landing/animated-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create features section</name>
  <files>src/components/landing/features.tsx</files>
  <action>
Create `src/components/landing/features.tsx`:
- Server component (no "use client" - static content)
- Import AnimatedSection from "./animated-section"
- Import Card, CardHeader, CardTitle, CardDescription from @/components/ui/card
- Import icons from lucide-react: Sparkles (AI), Zap (Speed), Image (Quality), Download (Export), Shield (Security), Clock (Time-saving)

Features array (6 features in 3x2 grid):
1. AI-Powered Generation - "Gemini creates compelling prompts, Kie.ai renders stunning visuals"
2. Lightning Fast - "From upload to download in under 5 minutes"
3. Professional Quality - "Thumbnails that stand out in the YouTube feed"
4. Easy Downloads - "Download individually or batch export as ZIP"
5. Secure Storage - "Your portraits and thumbnails stored safely"
6. Save Hours - "No design skills needed, no Photoshop required"

Layout:
- Section with id="features"
- Container with max-w-6xl mx-auto px-4
- Section heading: "Everything You Need" centered
- Subheading: "Professional thumbnails without the complexity"
- Grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Each card wrapped in AnimatedSection with staggered delay (index * 0.1)
- Cards use glass class
- Icon size: size-10, text-primary, mb-4
  </action>
  <verify>
   - File exists and has no TypeScript errors
   - `npx tsc --noEmit` passes
  </verify>
  <done>Features section displays 6 feature cards in responsive grid with animations</done>
</task>

<task type="auto">
  <name>Task 2: Create pricing section</name>
  <files>src/components/landing/pricing.tsx</files>
  <action>
Create `src/components/landing/pricing.tsx`:
- Server component for the section, but use AnimatedSection wrappers
- Import Card components, Button, Check icon, cn utility
- Import AnimatedSection from "./animated-section"
- Import Link from "next/link"

Pricing tiers array:
1. Free - $0/month
   - "Get started with 5 thumbnails"
   - Features: ["5 generations per month", "All AI models included", "HD downloads", "Basic support"]
   - CTA: "Start Free" -> /signup
   - highlighted: false

2. Pro - $19/month (or EUR 19)
   - "Perfect for active creators"
   - Features: ["50 generations per month", "All AI models included", "HD downloads", "Priority support", "Batch ZIP download"]
   - CTA: "Upgrade to Pro" -> /signup
   - highlighted: true (Most Popular badge)

3. Agency - $49/month (or EUR 49)
   - "For teams and power users"
   - Features: ["200 generations per month", "All AI models included", "HD downloads", "Priority support", "Batch ZIP download", "API access (coming soon)"]
   - CTA: "Go Agency" -> /signup
   - highlighted: false

Layout:
- Section with id="pricing"
- Section heading: "Simple, Transparent Pricing" centered
- Subheading: "Start free, upgrade when you're ready"
- Grid: grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto
- Highlighted tier: border-primary ring-2 ring-primary/20, "Most Popular" badge absolutely positioned at top
- Each tier card wrapped in AnimatedSection with staggered delay
- Price display: large text-4xl font-bold for price, text-muted-foreground for "/month"
- Feature list with Check icons (text-primary)
- Full-width CTA button at bottom: default variant for highlighted, outline for others
  </action>
  <verify>
   - File exists and has no TypeScript errors
   - Pro tier visually stands out with border/badge
  </verify>
  <done>Pricing section shows three tiers with correct prices and feature lists</done>
</task>

<task type="auto">
  <name>Task 3: Create testimonials and final CTA sections</name>
  <files>src/components/landing/testimonials.tsx, src/components/landing/cta.tsx</files>
  <action>
1. Create `src/components/landing/testimonials.tsx`:
   - Import AnimatedSection, Card components, Avatar components
   - Import Quote icon from lucide-react

   Placeholder testimonials (3):
   - "Finally, thumbnails that actually get clicks. This tool paid for itself in the first week." - Alex R., Gaming Channel
   - "I used to spend 2 hours per thumbnail. Now it's 5 minutes. Game changer." - Sarah M., Tech Reviewer
   - "The AI understands what YouTube thumbnails need to look like. Impressive results every time." - Mike T., Lifestyle Creator

   Layout:
   - Section with id="testimonials"
   - Section heading: "What Creators Are Saying"
   - Subheading: "Join thousands of YouTubers saving time"
   - Grid: grid-cols-1 md:grid-cols-3 gap-6
   - Each testimonial in a Card with glass class:
     - Quote icon (muted, top of card)
     - Quote text
     - Avatar (use initials placeholder via Avatar component) + Name + Channel type
   - Wrap each in AnimatedSection with stagger

2. Create `src/components/landing/cta.tsx`:
   - Client component for animation
   - Import { motion } from "motion/react"
   - Import Button, ArrowRight icon, Link

   Layout:
   - Section (not anchored, near bottom)
   - Glass card spanning full width with gradient overlay
   - Large heading: "Ready to Create Scroll-Stopping Thumbnails?"
   - Subheading: "Start generating professional thumbnails in minutes. No credit card required."
   - Two buttons: "Get Started Free" (primary, large) -> /signup, "View Pricing" (ghost) -> #pricing
   - Centered content, py-20
   - Animate the whole card on viewport entry
  </action>
  <verify>
   - Both files exist with no TypeScript errors
   - `npx tsc --noEmit` passes
  </verify>
  <done>Testimonials section shows 3 placeholder reviews, CTA section provides final conversion prompt</done>
</task>

<task type="auto">
  <name>Task 4: Integrate all sections into landing page</name>
  <files>src/app/page.tsx</files>
  <action>
Update `src/app/page.tsx`:
- Import all new components: Features, Pricing, Testimonials, CTA
- Replace placeholder divs with actual components
- Add a simple footer at the bottom with copyright

Final page structure:
```tsx
<main className="min-h-screen bg-background">
  <Navbar />
  <Hero />
  <Features />
  <Pricing />
  <Testimonials />
  <CTA />
  <footer className="py-8 text-center text-sm text-muted-foreground border-t">
    <p>&copy; {new Date().getFullYear()} YouTube Thumbnail Factory. All rights reserved.</p>
  </footer>
</main>
```

Ensure smooth section spacing:
- Each section should have consistent py-24 padding
- Sections alternate slightly in visual weight (Features has cards, Pricing has cards, Testimonials has cards, CTA is different)
  </action>
  <verify>
   - `npm run build` succeeds
   - All sections render on the landing page
   - Scroll navigation works to all sections
  </verify>
  <done>Landing page complete with all sections integrated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete landing page with Hero, Features, Pricing, Testimonials, and CTA sections</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open http://localhost:3000 in browser (while logged out)
3. Verify landing page loads with:
   - Fixed navbar with "Log In" and "Get Started" buttons
   - Animated hero section with headline and CTAs
   - Features section with 6 feature cards
   - Pricing section with Free/Pro/Agency tiers (Pro highlighted)
   - Testimonials section with 3 quotes
   - Final CTA section
   - Footer with copyright
4. Test smooth scrolling: Click "View Pricing" -> should scroll to pricing
5. Test navigation: Click "Get Started" -> should go to /signup
6. Test responsive: Resize to mobile width, verify layout adapts
7. (Optional) Log in and visit / again - should still show landing page
  </how-to-verify>
  <resume-signal>Type "approved" if landing page looks good and functions correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes without errors
- [ ] Features section displays 6 features in grid (LAND-02)
- [ ] Pricing section shows 3 tiers with correct prices and quotas (LAND-03)
- [ ] Testimonials section shows social proof (LAND-04)
- [ ] CTA section prompts signup
- [ ] All CTA buttons link to /signup
- [ ] Smooth scroll works for all anchor links
- [ ] Page is responsive on mobile
- [ ] Dark theme glass effect consistent throughout
</verification>

<success_criteria>
1. Landing page displays all required sections: hero, features, pricing, testimonials
2. Pricing clearly shows Free (5/mo), Pro ($19, 50/mo), Agency ($49, 200/mo)
3. All CTA buttons lead to signup flow
4. Visual design matches existing app aesthetic (dark theme, glass effects)
5. User confirms landing page looks good and functions correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-landing-page/05-02-SUMMARY.md`
</output>
