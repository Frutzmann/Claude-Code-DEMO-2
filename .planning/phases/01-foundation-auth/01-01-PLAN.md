---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/theme-provider.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/components/ui/label.tsx
  - src/components/ui/sonner.tsx
  - tailwind.config.ts
  - postcss.config.mjs
autonomous: true

must_haves:
  truths:
    - "App renders with dark theme by default"
    - "Toast notifications appear in top-right corner"
    - "UI components have consistent styling with glass effects"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout with providers"
      contains: "ThemeProvider"
    - path: "src/app/globals.css"
      provides: "Dark theme CSS variables"
      contains: "--color-background"
    - path: "src/components/theme-provider.tsx"
      provides: "Theme context wrapper"
      contains: "next-themes"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "import and wrap children"
      pattern: "ThemeProvider"
    - from: "src/app/layout.tsx"
      to: "src/components/ui/sonner.tsx"
      via: "Toaster component"
      pattern: "Toaster"
---

<objective>
Initialize the Next.js 15 project with TypeScript, Tailwind CSS v4, shadcn/ui, and dark theme foundation.

Purpose: Establish the base project structure and UI primitives that all subsequent plans will use.
Output: Working Next.js app with dark theme, toast notifications, and base UI components.
</objective>

<execution_context>
@/Users/frutz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/frutz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    tailwind.config.ts
  </files>
  <action>
    Create Next.js 15 project in current directory (not a subdirectory) with App Router and TypeScript.

    Run: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack --yes`

    Then install additional dependencies:
    `npm install @supabase/supabase-js @supabase/ssr next-themes react-hook-form zod @hookform/resolvers sonner lucide-react`

    The project should use:
    - TypeScript with strict mode
    - Tailwind CSS v4 with CSS-first config
    - App Router with src/ directory
    - @ import alias

    Note: If create-next-app fails because directory is not empty, move existing files temporarily or use --force if available.
  </action>
  <verify>
    Run `npm run dev` and visit http://localhost:3000 - should see default Next.js page
    Run `npm ls next` - should show Next.js 15.x
  </verify>
  <done>Next.js project initialized with all dependencies installed, dev server starts without errors</done>
</task>

<task type="auto">
  <name>Task 2: Setup dark theme and UI components</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/components/theme-provider.tsx
    src/components/ui/button.tsx
    src/components/ui/input.tsx
    src/components/ui/card.tsx
    src/components/ui/label.tsx
    src/components/ui/sonner.tsx
  </files>
  <action>
    1. Initialize shadcn/ui: `npx shadcn@latest init`
       - Style: New York
       - Base color: Zinc
       - CSS variables: yes

    2. Add required shadcn components:
       `npx shadcn@latest add button input card label sonner`

    3. Update `src/app/globals.css` with dark theme as default:
       - Use oklch color space for modern color handling
       - Define glass effect variables (--color-card with transparency)
       - Set dark theme colors per RESEARCH.md pattern
       - Add `.glass` utility class for glass morphism effect

    4. Create `src/components/theme-provider.tsx`:
       - Wrap NextThemesProvider from next-themes
       - Export as client component ("use client")

    5. Update `src/app/layout.tsx`:
       - Import and wrap children with ThemeProvider (defaultTheme="dark", enableSystem, disableTransitionOnChange)
       - Add Toaster from sonner (position="top-right")
       - Add suppressHydrationWarning to html tag
       - Set metadata (title: "YouTube Thumbnail Factory", description: appropriate)
       - Import Inter font from next/font/google

    6. Create `src/app/page.tsx` as a simple placeholder:
       - Just render a centered Card with "YouTube Thumbnail Factory" heading
       - Use glass effect styling to verify theme works
  </action>
  <verify>
    Run `npm run dev`:
    - Page should display with dark background
    - Card should have glass effect (semi-transparent with blur)
    - No hydration warnings in console
    Run `npm run build` - should complete without errors
  </verify>
  <done>Dark theme active by default, shadcn/ui components available, Toaster configured, glass effect working</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. Visit http://localhost:3000 - dark theme visible
3. `npm run build` completes successfully
4. No TypeScript errors in the codebase
</verification>

<success_criteria>
- Next.js 15 project running with TypeScript
- Tailwind CSS v4 with dark theme as default
- shadcn/ui components (button, input, card, label) available
- ThemeProvider wrapping app with dark default
- Toaster (sonner) configured and ready
- Glass effect utility class working
- All dependencies from RESEARCH.md installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
